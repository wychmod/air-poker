# UI

React-only presentation code lives here. UI modules should translate user intent
into domain actions instead of mutating game state directly.

Planned boundaries:

- `components/`
- `panels/`
- `screens/`
- `hooks/`
